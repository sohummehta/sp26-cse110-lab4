## Devtools part 2 answers

1. The bug is that num1 and num2 are retrieved as strings, so they are getting concatenated as strings instead of getting added as numbers. 
2. Wrapping lines 3-4 with Number() is how I would fix it. 